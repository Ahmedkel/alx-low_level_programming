#include "main.h"

/**
* factorial - computes the factorial of a given number.
*
* @n: input number.
*
* Return: the factorial of n or -1 if number is negative..
*/
int factorial(int n)
{
	return (n < 0 ? -1 : n == 0 ? 1 : n * factorial(ln - 1));
}
